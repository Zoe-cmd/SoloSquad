# 工作流补充流程

> **补充文档**：仅当遇到对应场景时阅读。核心工作流请参见 `workflow.md`。

---

## 异常处理流程

### 阶段回退

如果当前阶段发现问题需要回到前一个阶段：

```
当前阶段发现问题
    │
    ▼
判断问题类型
    │
    ├── 需求问题 → 回到 Product Manager
    ├── 设计问题 → 回到 UI/UX Designer 或 Solution Architect
    ├── 数据问题 → 回到 Database Engineer
    ├── 实现问题 → 回到对应 Backend/Frontend/AI Engineer
    ├── 质量问题 → 回到对应 Engineer 修复
    ├── 安全问题 → 回到对应 Engineer 修复
    └── 部署问题 → 回到 DevOps Engineer 修复
```

### 回退流程

1. 当前 Agent 识别问题并记录在 Decision Log
2. 更新 Todo 状态，标记需要回退的任务
3. 通知前序 Agent 重新启动
4. 前序 Agent 读取问题描述
5. 前序 Agent 修复问题
6. 前序 Agent 更新交付物
7. 前序 Agent 重新交接

### 紧急回退

当发现严重问题（Critical Bug、安全漏洞）时：

1. 立即通知 技术负责人 和 Human Developer
2. 技术负责人评估影响范围
3. Human Developer 决定是否回退
4. 执行紧急回退流程
5. 更新 Decision Log

---

## 需求变更流程

### 变更入口

**所有需求变更必须通过项目经理**，不得直接告诉执行角色。这是确保变更被正确记录和传达的唯一方式。

### 变更分级

| 变更级别 | 定义 | 审批人 | 流程 |
|----------|------|--------|------|
| **L1 - 简单变更** | 增删功能、调整UI、修改文案 | 项目经理 | 人类 → 项目经理 → 执行 |
| **L2 - 复杂变更** | 修改技术方案、影响架构、修改数据库Schema | 项目经理 + 技术负责人 | 人类 → 项目经理 → 技术负责人 → 执行 |
| **L3 - 紧急变更** | 生产Bug、安全漏洞、阻塞性问题 | 项目经理（先执行后补审） | 人类 → 项目经理 → 立即执行 → 事后补审 |

### 变更级别判定规则

```
项目经理收到变更需求
    │
    ├── 是否影响现有架构？
    │   ├── 是 → L2（需要技术负责人）
    │   └── 否 ↓
    │
    ├── 是否需要修改数据库Schema？
    │   ├── 是 → L2（需要技术负责人）
    │   └── 否 ↓
    │
    ├── 是否影响多个模块？
    │   ├── 是 → L2（需要技术负责人）
    │   └── 否 ↓
    │
    ├── 是否是紧急问题？
    │   ├── 是 → L3（先执行后补审）
    │   └── 否 → L1（项目经理直接决定）
```

### 变更流程

```
人类 → 项目经理（唯一入口）
    │
    ├── 1. 记录变更
    │   ├── 写入 docs/变更总览.md（一行总结 + 相关角色）
    │   ├── 写入 docs/变更记录/变更-{日期}-{序号}.md（详细文档）
    │   └── 判断变更级别
    │
    ├── 2. 审批变更
    │   ├── L1：项目经理直接决定
    │   ├── L2：项目经理 + 技术负责人评估
    │   └── L3：立即执行，24小时内补审
    │
    ├── 3. 通知相关角色
    │   ├── 更新 docs/变更总览.md 状态
    │   └── 通知相关角色读取变更文档
    │
    └── 4. 执行变更
        ├── 相关角色读取变更文档
        ├── 执行变更
        ├── 更新文档
        └── 交接
```

### 紧急变更流程

```
紧急变更触发
    │
    ├── 触发条件
    │   ├── 生产环境 Bug
    │   ├── 安全漏洞
    │   ├── 阻塞性问题（导致其他角色无法继续）
    │   └── 人类明确要求「紧急处理」
    │
    ├── 快速通道
    │   ├── 人类直接告诉项目经理「这是紧急变更」
    │   ├── 项目经理口头批准
    │   ├── 执行角色立即执行
    │   ├── 24小时内补写变更文档
    │   └── 项目经理确认并归档
    │
    └── 通知机制
        ├── 执行角色立即通知受影响的角色
        ├── 项目经理更新变更总览文档
        └── 相关角色读取变更文档
```

### 变更回滚流程

```
变更回滚触发
    │
    ├── 触发条件
    │   ├── 变更执行后发现问题
    │   ├── 变更导致其他功能异常
    │   ├── 人类要求回滚
    │   └── 测试不通过
    │
    ├── 回滚级别
    │   ├── L1 回滚（代码级）：Git revert
    │   ├── L2 回滚（架构级）：需要技术负责人评估
    │   └── L3 回滚（数据级）：需要数据库工程师协助
    │
    ├── 回滚流程
    │   ├── 执行角色识别回滚范围
    │   ├── 项目经理批准回滚
    │   ├── 执行回滚操作
    │   ├── 通知所有受影响角色
    │   ├── 更新变更状态为「已回滚」
    │   └── 更新变更总览文档
    │
    └── 回滚文档
        └── 在原变更文档中追加「回滚记录」章节
```

### 变更总览文档

**必读文档**：所有角色在启动时必须阅读 `docs/变更总览.md`，了解项目变更历史。

变更总览文档格式：

```markdown
# 变更总览

> **必读文档**：所有角色在启动时必须阅读本文档，了解项目变更历史。

## 变更列表

| 编号 | 日期 | 标题 | 级别 | 状态 | 相关角色 |
|------|------|------|------|------|----------|
| CHG-001 | 2024-01-01 | 添加用户登录功能 | L1 | 已完成 | 后端工程师、前端工程师 |
| CHG-002 | 2024-01-02 | 修复登录Bug | L3 | 已完成 | 后端工程师 |

## 变更详情

### CHG-001: 添加用户登录功能

- **一句话总结**：为系统添加用户登录和注册功能
- **详细文档**：`docs/变更记录/变更-2024-01-01-001.md`
- **相关角色**：后端工程师、前端工程师

## 角色变更索引

### 后端工程师相关变更

- [x] CHG-001: 添加用户登录功能（已完成）

### 前端工程师相关变更

- [x] CHG-001: 添加用户登录功能（已完成）
```

---

## 角色依赖管理

### 依赖类型

| 依赖类型 | 示例 | 说明 |
|----------|------|------|
| **前置依赖** | 数据库工程师 → 后端工程师 | 后端需要数据库 Schema |
| **并行关系** | 前端工程师 ↔ AI 工程师 | 可以同时进行 |
| **条件依赖** | AI 工程师（仅在需要 AI 功能时） | 某些角色可选 |
| **反馈依赖** | 测试工程师 → 后端工程师 | 测试发现问题需要后端修复 |

### 角色依赖矩阵

#### 前置依赖

| 目标角色 | 依赖角色 | 依赖产出物 | 依赖类型 |
|----------|----------|------------|----------|
| 后端工程师 | 数据库工程师 | `docs/数据库设计文档.md` | 必需 |
| 前端工程师 | 后端工程师 | `docs/API规范文档.md` | 必需 |
| 测试工程师 | 前端工程师 | 前端代码 | 必需 |
| 安全工程师 | 后端工程师 | 后端代码 | 必需 |
| 代码评审工程师 | 安全工程师 | `docs/安全审计报告.md` | 必需 |
| 运维工程师 | 代码评审工程师 | `docs/代码审查报告.md` | 必需 |

#### 可并行执行

| 角色组 | 说明 |
|--------|------|
| 前端工程师 + AI 工程师 | 依赖不同，可并行 |
| 测试工程师 + 安全工程师 | 可并行（但需注意代码版本一致） |

#### 条件依赖（根据团队规划决定）

| 角色 | 条件 | 跳过时的影响 |
|------|------|--------------|
| UI/UX 设计师 | 需要自定义 UI 设计 | 使用现成组件库 |
| 数据库工程师 | 需要关系型数据库 | 使用文件存储 |
| AI 工程师 | 需要 AI 功能 | 无 AI 功能 |
| 安全工程师 | 涉及敏感数据 | 跳过安全审计 |
| 运维工程师 | 需要 CI/CD | 手动部署 |

### 动态依赖解析

在 Phase 0 的项目经理工作中，新增「依赖解析」步骤：

1. 团队规划完成后
2. 根据团队配置，解析角色依赖
3. 生成执行顺序
4. 识别可并行执行的角色组
5. 在 Todo 中标记依赖关系

---

## 时间估算

### 各阶段预计时间

| 阶段 | 角色 | 预计时间 | 依赖 |
|------|------|----------|------|
| Phase 0 | 项目经理 | 0.5-1 小时 | 无 |
| Phase 0 | 技术负责人 | 0.5-1 小时 | 项目经理 |
| Phase 1 | 产品经理 | 1-2 小时 | Phase 0 |
| Phase 1 | UI/UX设计师 | 1-2 小时 | 产品经理 |
| Phase 1 | 系统架构师 | 1-2 小时 | UI/UX设计师 |
| Phase 2 | 数据库工程师 | 1-2 小时 | 系统架构师 |
| Phase 2 | 后端工程师 | 2-4 小时 | 数据库工程师 |
| Phase 3 | 前端工程师 | 2-4 小时 | 后端工程师 |
| Phase 3 | AI工程师 | 1-3 小时 | 后端工程师 |
| Phase 4 | 测试工程师 | 1-2 小时 | Phase 3 |
| Phase 4 | 安全工程师 | 1-2 小时 | 测试工程师 |
| Phase 4 | 代码评审工程师 | 1-2 小时 | 安全工程师 |
| Phase 5 | 运维工程师 | 1-2 小时 | Phase 4 |
| Phase 5 | 项目经理 | 0.5-1 小时 | 运维工程师 |

**总计**: 约 14-28 小时（取决于项目复杂度）

---

## 附录C: 文档流转图

```
project-plan.md ──────────────────────────────────────────────────┐
    │                                                              │
    ▼                                                              │
prd.md ───────────────────────────────────────────────────────────┤
    │                                                              │
    ├──▶ design-system.md ────────────────────────────────────────┤
    ├──▶ user-flows.md ───────────────────────────────────────────┤
    └──▶ wireframes.md ───────────────────────────────────────────┤
            │                                                      │
            ▼                                                      │
    architecture.md ──────────────────────────────────────────────┤
            │                                                      │
            ├──▶ database-schema.md ──────────────────────────────┤
            │        │                                             │
            │        ▼                                             │
            │    api-spec.md ─────────────────────────────────────┤
            │        │                                             │
            │        ├──▶ frontend code ──────────────────────────┤
            │        ├──▶ ai code ────────────────────────────────┤
            │        │                                             │
            │        ▼                                             │
            │    test-plan.md ──▶ test-report.md ─────────────────┤
            │    security-audit-report.md ────────────────────────┤
            │    code-review-report.md ───────────────────────────┤
            │        │                                             │
            │        ▼                                             │
            │    deployment-plan.md ──────────────────────────────┤
            │        │                                             │
            │        ▼                                             │
            └──▶ project-summary.md ◀─────────────────────────────┘
```